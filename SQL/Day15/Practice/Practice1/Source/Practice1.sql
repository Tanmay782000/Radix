CREATE DATABASE MODULE15

USE MODULE15

CREATE TABLE EMPLOYEE(
ID INT PRIMARY KEY,
NAME VARCHAR(100),
DEPARTMENT VARCHAR(100),
SALARY BIGINT
)

INSERT INTO EMPLOYEE VALUES(1,'TANMAY','.NET',60000)

CREATE TABLE PF(
ID INT,
PF_AMOUNT BIGINT
)
INSERT INTO PF VALUES(1,0)

BEGIN TRAN MYTRAN
UPDATE EMPLOYEE 
SET SALARY = SALARY - 4000
WHERE ID = 1

UPDATE PF 
SET PF_AMOUNT = PF_AMOUNT + 4000
WHERE ID = 1

COMMIT TRAN MYTRAN
SELECT * FROM EMPLOYEE
SELECT * FROM PF


